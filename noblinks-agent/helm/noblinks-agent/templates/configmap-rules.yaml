apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "noblinks-agent.fullname" . }}-prometheus-rules
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "noblinks-agent.labels" . | nindent 4 }}
data:
  noblinks.yml: |
    groups:
      - name: noblinks
        rules: []
